services:
  - type: web
    name: eval-rails
    env: docker
    plan: free
    region: oregon
    envVars:
      - key: RAILS_ENV
        value: production
      - key: RAILS_LOG_TO_STDOUT
        value: true
      - key: GEMINI_API_KEY
        sync: false
